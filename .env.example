# Local dev: SQLite file
DATABASE_URL="file:./dev.db"

# Production (Turso): full libsql:// URL with the auth token embedded as a
# query param.
# DATABASE_URL="libsql://<db>-<org>.turso.io?authToken=<token>"

# Base URL used for metadata/sitemap generation
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# 중앙선거관리위원회 공공데이터포털 API 일반 인증키 (data.go.kr)
NEC_API_SERVICE_KEY=""

# Random secret used to sign login session cookies. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=""
